export default function Page() {
    return <div>
        <p>This demo shows what is a difference between using nextjs middleware and calling API direcly</p>
        <p>Open developer tools and browse to Posts and Posts Sync</p>
        <p>Posts uses react server components where as SyncPosts uses client side rendering</p>
        </div>

}